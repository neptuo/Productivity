<UserControl x:Class="Neptuo.Productivity.VisualStudio.UI.Builds.HistoryOverviews.QuickView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:data="clr-namespace:Neptuo.Productivity.VisualStudio.UI.Builds.Converters"
             x:Name="Root"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource VsBrush.WindowText}" />
        </Style>
        <data:BuildLengthConverter x:Key="buildLengthConverter" />
        <data:DateTimeConverter x:Key="dateTimeConverter" />
        <data:FirstLetterConverter x:Key="firstLetterConverter" />
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <ItemsControl ItemsSource="{Binding Builds}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0 0 0 1" BorderBrush="{DynamicResource VsBrush.PanelSeparator}">
                            <Grid Margin="0 5 0 5">
                                <Rectangle Fill="#15C23C" Width="{Binding ElapsedMilliseconds, Converter={StaticResource buildLengthConverter}}" VerticalAlignment="Stretch" HorizontalAlignment="Right" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                
                                    <Label Grid.Column="0" Content="{Binding StartedAt, Converter={StaticResource dateTimeConverter}}" />
                                    <Label Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Content="{Binding Scope }" />
                                    <Label Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Content="{Binding Action}" />
                                    <Label Grid.Column="3" Content="{Binding Description}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
